1. 异常描述: 调用DLL函数，出现错误 Run-Time Check Failure #0 xx
   原因分析：在项目中调用dll 导出的函数,参数不对导致的。
   解决方案: 进行调试，传入正确的参数。
  
2. 异常描述: 一个dll 通过http 将base64 字符串 传送给 另个一进程，这个进程接收到的base64 中有空格，导致验签失败。
   原因分析：在Url 编码的过程中，会将 ‘+’ 转成 ‘ ’。
   解决方案: 在通过url 传输base64字符串之前，先将base64 字符串 url encode 一下。
